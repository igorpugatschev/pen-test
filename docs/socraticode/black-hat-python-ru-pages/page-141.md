# Black Hat Python. Программирование для хакеров и пентестеров — страница 141

Источник: `Black Hat Python. Программирование для хакеров и пентестеров.pdf`

Подбор паролей на основе содержимого веб-сайта   141
        self.context    = None
        self.hosts      = set()
        # Начинаем с пароля, который, как мы знаем, часто встречается
        self.wordlist = set(["password"]) 
        # подготавливаем свое расширение
        callbacks.setExtensionName("BHP Wordlist")
        callbacks.registerContextMenuFactory(self)
        return
    def createMenuItems(self, context_menu):
        self.context = context_menu
        menu_list = ArrayList()
        menu_list.add(JMenuItem(
            "Create Wordlist", actionPerformed=self.wordlist_menu))
        return menu_list
Вам уже должен быть знаком код, приведенный в этом листинге. Вначале
мы импортируем необходимые модули. Вспомогательный класс TagStripper
позволит убрать HTML-теги из HTTP-ответов, которые мы позже будем об -
рабатывать. Его метод handle_data сохраняет текст страницы в переменную
page_text . Мы также определяем метод handle_comment, поскольку нужно
также добавить в список паролей слова из комментариев разработчиков.
На самом деле handle_comment просто вызывает handle_data  (на случай,
если позже захочется по-другому обрабатывать текст страницы).
Метод strip передает HTML-код базовому классу , HTMLParser, и возвращает
итоговый текст страницы , который нам пригодится позже. Все остальное
почти идентично начальной части скрипта bhp_bing.py, который мы только
что дописали. Как и прежде, мы хотим создать пункт в контекстном меню
пользовательского интерфейса Burp. Из нового здесь только то, что словарь
хранится в множестве (set), благодаря чему удастся избежать добавления
одинаковых слов. Мы инициализируем это множество с помощью всеми
любимого пароля password , просто чтобы не забыть внести его в итоговый
список.
Т еперь добавим логику , которая будет превращать HTTP-трафик, выбранный
нами в Burp, в базовый словарь:
def wordlist_menu(self,event):
    # извлекаем подробности о том, по чему щелкнул пользователь
    http_traffic = self.context.getSelectedMessages()
