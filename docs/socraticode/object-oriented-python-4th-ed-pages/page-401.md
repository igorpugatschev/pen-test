# Объектно-ориентированный Python, 4-е издание — страница 401

Источник: `_media_books_obektno-orientirovannyj-python-4-izd.pdf`

400 ГЛАВА 9 Строк и , сер иали за ци я и пут и к фа йла м
Нотация среза может испо льзова ться в by tearr ay для изменения элемента
на месте без дополните льных затрат на создан ие нового объекта. Например,
следующий код создает bytearr ay из объекта bytes, а затем заменяет два байта:
>» Ьа = by tearr ay (b " abcde fgh ")
>» Ьа [ 4 :6] = Ь" \х15\х а3"
»> Ьа
by tearra y(b' abcd\x15 \xa3gh ')
Здесь использована нотация среза для замены байтов в срезе [ 4: 6] двумя за­
мещающими байтам и Ь" \х15\ха3 ".
Если манипу лир овать одним элементом в bytearr ay, значение должно быть
целым числом от О до 255 (вк лючительно), что является опре деле нным ша­
блоном bytes. Если поп ытаться передать сим вол или объект bytes, это вызовет
исключение.
Одноба йтный сим вол может быть пр еобразован в целое число с помощью
функции ord () (с окращение от порядковою номера). Функция ord () возвращает
целочисленное представл ение одного символа:
»> Ьа = by tearr ay (b" abcde fg h")
>» Ьа [З] = or d(b 'g' )
»> Ьа [4] = 68
»> Ьа
byte arr ay (b' abcg Dfgh ')
После создания массива символ с индексом 3 (че твертый символ, так как индек­
сация начинается с 0, как и в списк ах) заменяется байтом 103. Это целое число
было возвращено функцией ord () и является сим волом AS CII для строчной
буквы g.
Для ясности мы также заменили следующий сим вол байтом с номером 68, со­
ответс твующим сим волу ASCII для заглавной буквы D.
Тип Bytearr ay содержит мето ды, позво ляющие ему вести себя как спис ок (н а­
пример, можно добавить к нему целоч исленные значения байт ов). Он также
может вести себя как объект bytes (мо жно испо льзова ть такие мето ды, как
count () и fin d( ) ). Ра зница в том, что Bytearr ay - изменяемый тип, поле зный
для создания сложн ых последова тельностей байтов из конкретного входного
источ ника. Например, мы можем проч итать заго ловок с четырьмя байтам и
с информацией о длине, пр ежде чем считыва ть байты полезной нагру зки.
Очень полезно иметь возмо жность выпо лнять чтение непосредс твенно в из­
меняемый Bytea rr ay, чтобы создава ть и сохранять множество небо льших
объектов в памяти .
