# Объектно-ориентированный Python, 4-е издание — страница 231

Источник: `_media_books_obektno-orientirovannyj-python-4-izd.pdf`

230 ГЛАВА 6 Абстр актны е класс ы и перегру зка операто ров
Для нечетных чисел применено деление по модулю. Если остаток от деления х
на 2 равен нулю, то х - четное число, иначе - нечетное.
Ра ссмотрим след ующий пример: созда дим экземпляр объекта OddContainer
и убедимся, что, хотя мы и не расширяли Contain er, класс ведет себя подобно
объекту Contain er:
>>> odd = Od din tegers ()
>>> is instance (o dd, Con tainer)
True
>>> is subclas s(O ddi nteg ers , Con ta1ner)
True
Вот именно поэтому утиная тип изация намного лучше классического поли мор­
физма. Реа льно создавать отношения is-a, не тратя времени на написание кода
для настройки наследования (или множеств енного наследования).
Особенность АБС Container заключается в том, что любой класс, реализующий
его, может испо льзовать ключевое слово in. На самом деле in - это обычный
синтаксический сахар, котор ый обращ ается к методу _c ontains _( ). Любой
клас с, имеющий метод _con tains _( ), является Container и поэтому может
быть запрошен с помощью ключевого слова in. Например:
Полезное преиму щество во всем этом - возможность создавать новые типы
коллекц ий, которые полностью совмес тимы со встро енными универсальными
коллекциями Python. Например, создать словарь, использующий структуру дво­
ичного дерева, для хранения ключей вместо хешированн ого поиска. Достаточно
опре делить абстрак тный базовый класс Mapping и изменить алгоритм ы, под­
держивающие такие методы, кaк _g et item_( ), _setit em_( ) и _d el it em_( ).
Утиная типизация Python работает (ч астично) посредством встроен ных функ­
ций isins tance () и issubclas s( ), которые служат для определения отношений
классов. Они ссылаются на два внутренних метода, которые могут предоставлять
клaccы: _i nstancecheck _( ) и _subc lassch eck _( ). Класс АБС предостав ляет
мeтoд _s ubclass hook_( ), используемый методом _s ubclassch eck_( ) для под­
тверждения того, что данный класс является подклассом абстрактного базовог о
класса. В книге мы нс будем подробно разбирать абстрактные классы. Но все, что
здесь описано, можно считать инструкцией и следовать ей при создании новых
классов, которые должны тесно работать со встро енными классам и.
