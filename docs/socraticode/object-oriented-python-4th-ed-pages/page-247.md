# Объектно-ориентированный Python, 4-е издание — страница 247

Источник: `_media_books_obektno-orientirovannyj-python-4-izd.pdf`

246 ГЛАВА 6 Абстр ак тны е класс ы и перег рузка операто ров
Поск ольку Python в основном манипулирует специа льными методам и, для до­
бавления функции мы можем изменить их пове дение. Например, перегрузить
операторы новыми типами данных. Один из приме ров - модуль pathlib :
>>> from pa thlib impor t Path
>>> home = Path . hom e()
»> home / "min icond aЗ" / "e nvs "
PosixPa th( ' /User s/sl ott /m1n1 conda З/envs ')
&. Резу л�таты будут разл и ча ться в зав иси мос ти от ис пользуе мо й о пераци­
- о нн о и сис те мы и и мен и пользо ва тел я.
Неизменным остается то, что оператор / служит для соединения объекта Path
со строковыми объект ами, создава я новый объект Path.
Оп ератор / реализуется мeтoдaми _t ruediv_( ) и_ rruediv _( ). Чтобы сделать
опе рации комм утати вными, в Python есть два места для поиска реализации.
Учиты вая выражение А ор в, где ор - любой из операторов Python, например
_add_ для +, Python выпо лняет следующие проверки спе циальных методов
реализации оператора.
1. Возмо жен вариант, когда в является подходя щим подклассом А. В этих ред­
ких случаях порядок меняется на обра тный, поэтому B. _rop _(A) можно
испо льзова ть в любом порядке. Это позво ляет подклассу в переоп ределить
операцию суп еркласса А.
2. Испо льзуйте А._ ор_(В). Если возвра щается значение, отличное от спе­
циальног о значения Notimplemented, это и есть результат. Для вы ражения
объекта Path, такого как home/ "miniconda З", эффек тивно ока зыва ется
home._ tr uediv_ ( "minicondaЗ "). Новый объект Path создается из старог о
объекта Path и стро ки.
3. Испо льзуйте в ._ rop_(A) . Это может быть метод _rаdd _( ) для реализа­
ции обратного сложения. Если возвращается значение, отличное от значе­
ния Notlmple mented, это и есть результат. Обра тите внимание, что порядок
оп ерандов обра тный. Для комму тати вных опера ций, таких как сложение
и умножение, порядок не имеет значения. Для некомму тативных опе раций,
таких как вычитание и деление, изменение порядка должно быть отражено
в реализации.
Вернемся к примеру создания симулятора игры в кости. Чтобы добавить экзем­
пляр Die в коллекцию Dice, можно реализовать оператор +. Сна чала опре делим
