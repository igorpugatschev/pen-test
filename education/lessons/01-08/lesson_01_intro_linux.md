# Занятие 1: Введение в Linux для пентестера

## Теория

Linux — основная ОС для пентестера. Дистрибутив Kali Linux включает готовый набор инструментов для тестирования на проникновение.

### Основные понятия

**Дистрибутивы Linux:**
- Kali Linux — специализированный дистрибутив для пентеста (основан на Debian)
- Parrot Security — альтернатива Kali с акцентом на приватность
- Ubuntu/Debian — для общего использования и серверов

**Файловая система Linux:**
- Все начинается с корня `/`
- Нет дисков C:, D: — всё в едином дереве
- Регистр имеет значение: `File.txt` и `file.txt` — разные файлы

**Ключевые директории:**
- `/etc` — конфигурационные файлы системы и служб
- `/var` — изменяемые данные (логи, кэш, почта, базы данных)
- `/home` — домашние директории пользователей
- `/root` — домашняя директория суперпользователя
- `/bin`, `/usr/bin` — исполняемые файлы программ
- `/tmp` — временные файлы (часто слабые права доступа)
- `/proc`, `/sys` — виртуальные файловые системы (информация о процессах и ядре)

**Виртуализация:**
- VMware Workstation / VMware Fusion
- VirtualBox (бесплатный, но на Mac с процессором M2 рекомендуется использовать **UTM** или **Parallels Desktop** для лучшей производительности)
- Прямой запуск на железе или в облаке

> **Примечание для пользователей macOS (M2, 8GB RAM):** При запуске виртуальных машин Kali Linux на Mac с 8GB оперативной памяти, выделяйте виртуалке не более **3-4GB RAM**, чтобы система не тормозила.

## Практическое занятие

### Шаг 1: Установка Kali Linux в VM

1. Скачать образ Kali Linux (VirtualBox/VMware) с официального сайта kali.org
2. Создать новую виртуальную машину:
   - Тип: Linux, версия: Debian (64-bit)
   - ОЗУ: минимум 2 ГБ (рекомендуется 4 ГБ, но на хосте с 8GB выделяйте ВМ 3-4GB)
   - Диск: 20-40 ГБ (динамический)
3. Загрузить VM с ISO-образа
4. Выбрать графическую установку (Graphical Install)
5. Настроить сеть, пароль root, создать пользователя
6. Завершить установку и перезагрузиться

### Шаг 2: Первичная настройка

```bash
# Обновление системы
sudo apt update && sudo apt full-upgrade -y
# Для macOS (если используется Homebrew): brew install <пакет>

# Установка базовых инструментов
sudo apt install -y vim curl wget git

# Проверка версии Kali
cat /etc/os-release
```
**Пример вывода `cat /etc/os-release`:**
```text
PRETTY_NAME="Kali GNU/Linux Rolling"
NAME="Kali GNU/Linux"
ID=kali
VERSION="2024.1"
VERSION_ID="2024.1"
ID_LIKE=debian
```

### Шаг 3: Изучение файловой системы

```bash
# Перейти в корень и посмотреть структуру
cd /
ls -la
```
**Пример вывода `ls -la` (корень):**
```text
total 80
drwxr-xr-x  1 root root 4096 Jan 10 10:00 .
drwxr-xr-x  1 root root 4096 Jan 10 10:00 ..
lrwxrwxrwx  1 root root    7 Jan 10 10:00 bin -> usr/bin
drwxr-xr-x  1 root root 4096 Jan 10 10:00 boot
drwxr-xr-x  1 root root 4096 Jan 10 10:00 dev
drwxr-xr-x  1 root root 4096 Jan 10 10:00 etc
drwxr-xr-x  1 root root 4096 Jan 10 10:00 home
lrwxrwxrwx  1 root root    7 Jan 10 10:00 lib -> usr/lib
drwx------  1 root root 4096 Jan 10 10:00 root
drwxr-xr-x  1 root root 4096 Jan 10 10:00 usr
drwxr-xr-x  1 root root 4096 Jan 10 10:00 var
```

```bash
# Изучить содержимое ключевых директорий
ls /etc/
ls /var/
ls /home/

# Посмотреть логи системы
ls /var/log/

# Найти конфиг сети
ls /etc/network/
cat /etc/hostname
cat /etc/hosts
```

### Шаг 4: Навигация по файловой системе

```bash
# Где я сейчас?
pwd

# Перейти в домашнюю директорию
cd ~
pwd

# Перейти в /etc
cd /etc
pwd

# Вернуться назад
cd ..
pwd

# Перейти в домашнюю директорию пользователя root
cd /root
# (если нет прав, используйте sudo -i)
```


## Примеры вывода

Пример вывода команд будет добавлен индивидуально для каждого урока.



## Адаптация под macOS (M2, 8GB)

- Для установки инструментов используйте Homebrew: `brew install <tool>`
- На MacBook Air M2 (8GB) запускайте VM с памятью не более 3-4GB
- Используйте UTM вместо VirtualBox (лучшая поддержка ARM)
- Docker работает нативно на M2: `docker pull <image>`
- Для VPN используйте Tunnelblick (OpenVPN) или официальные клиенты
- Для Python используйте `pip3 install` вместо `pip install`


## Задачи для самостоятельного выполнения

1. Установите Kali Linux в виртуальную машину и создайте снимок (snapshot) чистой системы. *Примечание: на Mac M2 рекомендуется использовать UTM.*

2. Изучите содержимое директории `/etc`. Найдите файлы: `passwd`, `shadow`, `group`, `hosts`, `resolv.conf`. Опишите назначение каждого файла в текстовом файле `etc_files.txt`.

3. Создайте структуру директорий в своем домашнем каталоге:
   ```
   ~/pen-test/
   ├── targets/
   ├── tools/
   ├── reports/
   └── scripts/
   ```
   Используйте команду `mkdir -p` для создания вложенных директорий.

4. *(Перенесено в Урок 6: Поиск файлов)*.
   *Справка:* Для поиска файлов по времени изменения используется команда `find`. Мы изучим её подробно в Уроке 6. Пока просто изучите содержимое `/var/log/` через `ls -lt`.

5. Выведите полный путь к исполняемому файлу команды `python3` с помощью команды `which` и `whereis`. Сравните результаты.

## Частые ошибки

1. **Путаница с регистром.** В Linux `File.txt` и `file.txt` — это разные файлы. Всегда проверяйте регистр при вводе путей.
2. **Забытое обновление системы.** Всегда выполняйте `sudo apt update && sudo apt full-upgrade -y` после установки, чтобы получить свежие патчи безопасности.
3. **Отсутствие снимков (snapshots) VM.** Перед экспериментами с пентестингом всегда делайте снимок виртуальной машины, чтобы можно было откатиться назад.

## Вопросы на понимание

1. Почему в Linux нет привычных дисков C: и D:, как в Windows?
2. В какой директории обычно хранятся конфигурационные файлы служб?
3. Чем отличается домашняя директория обычного пользователя от `/root`?
4. Зачем пентестеру использовать именно Kali Linux, а не обычный Ubuntu?
