# Занятие 2: Терминал Linux — базовые команды

## Учебная рамка

**Входные требования:** Выполнен урок 1: создан `~/slider-ai-pentest`, студент понимает разницу между macOS native, Kali VM и cloud lab.

**Результат занятия:** Студент уверенно перемещается по папкам, создает и читает файлы, копирует и перемещает артефакты, безопасно использует перенаправление и пайпы.

**Наследуемая SDET-компетенция:** воспроизводимые команды, работа с артефактами тестов, чтение логов, аккуратная организация результатов.

**Security QA-компетенция:** терминальная дисциплина для evidence: где выполнена команда, какой файл создан, что можно приложить к отчету и что нельзя сохранять.

**Связь с книгами:** материал урока составлен на основе «Легкий способ выучить Python 3 еще глубже» — терминал, аргументы командной строки, `cat`, перенаправление, 45-минутная практика и записная книжка; «PyCharm. Профессиональная работа на Python 2024» — рабочий проект, Git/Python tooling и различия локальной и Linux-среды.

**Основной источник:** «Легкий способ выучить Python 3 еще глубже» и «PyCharm. Профессиональная работа на Python 2024».

**Дополнительные источники:** «Объектно-ориентированный Python» для идеи аккуратной структуры проекта и разделения ответственности между папками.

**Что берем из источника:** готовую методику работы: маленькая практическая задача, наблюдение за собственным процессом, работа с файлами через терминал, проверка результата по фактическому выводу.

**Как это превращается в SDET/Security QA навык:** студент превращает команды терминала в управляемый workflow: подготовил файл, выполнил проверку, сохранил evidence, прочитал результат, не повредил чужие данные.

**Что нельзя переносить на Slider AI без отдельного разрешения:** удаление или изменение данных на стенде, массовые запросы, автоматизированный перебор, сохранение секретов из браузера или приложения.

**Процессный артефакт:** `Terminal evidence checklist` — список команд и правил, по которым студент фиксирует локальные результаты.

**Безопасная цель:** только локальная папка `~/slider-ai-pentest`. В этом уроке работа с Slider AI ограничена подготовкой локального файла заметок.

**Среда выполнения:** основной путь — macOS Terminal. Kali ARM64 VM допустима как углубление для сравнения поведения команд. Cloud lab не нужен.

**Обязательный путь новичка:** выполнить команды `pwd`, `ls`, `cd`, `mkdir`, `touch`, `cp`, `mv`, `cat`, `less`, `head`, `tail`, `grep`, `wc`, `sort`, безопасно применить `>` и `>>` на локальных файлах.

**Углубление:** сравнить отличия `ls`, `stat`, путей и системных папок между macOS и Kali/Ubuntu.

**Минимальная проверка успеха:** создан файл `notes/lesson_02_terminal.md`, создана папка `evidence/lesson_02`, есть вывод минимум пяти команд и объяснение каждой.

**Эталонный вывод:** студент показывает локальный файл evidence, где есть команда, вывод и строка «что это подтверждает».

**Критерии сдачи:** зачет — команды выполнены на локальных файлах; хорошо — студент объясняет пайп и перенаправление; отлично — добавлен безопасный чек-лист перед `rm`, `mv` и перезаписью файла.

## Reading pack из книг курса

Книги курса использованы автором как источники для построения этой лекции, но не как обязательное домашнее чтение. Студенту не нужно искать недостающую теорию в отдельных файлах или внешних материалах: все понятия, команды, ограничения, безопасные примеры, ожидаемые результаты и критерии сдачи для темы `Занятие 2: Терминал Linux — базовые команды` должны быть понятны из текущего урока.

В этом уроке книжные идеи переведены в учебную форму: сначала объясняется модель темы, затем показывается безопасная демонстрация, затем студент выполняет практику и оформляет evidence. Если книга описывает потенциально опасную технику, в курсе она используется только как lab-only или defensive interpretation и не переносится на Slider AI olddev без отдельного approval.


## Source-driven theory

Этот раздел не является заданием найти теорию в книгах. Книги использованы автором курса как системные источники для лекции `Занятие 2: Терминал Linux — базовые команды`, а студент получает полное объяснение ниже.

Для этой темы опорная идея взята из источников: «PyCharm. Профессиональная работа на Python 2024», «Легкий способ выучить Python 3 еще глубже», «Объектно-ориентированный Python». Из них в урок перенесены не страницы как домашнее чтение, а инженерные принципы: рабочая среда, терминальная дисциплина, файловая система, команды, evidence и аккуратная работа с локальными артефактами. Поэтому лекция строится вокруг вопроса: как SDET, уже умеющий работать с тестами, артефактами и воспроизводимостью, превращает тему `Занятие 2: Терминал Linux — базовые команды` в безопасную Security QA-практику.

Книжный материал адаптирован в три слоя. Первый слой — модель: какие сущности участвуют, как они связаны и где появляется риск. Второй слой — рабочий навык: воспроизводимые действия, контроль рабочей директории, хранение артефактов тестирования, чтение вывода и логов. Третий слой — границы применения: macOS native, локальные папки, локальные файлы и учебные заметки; Slider AI используется только как объект планирования evidence. Если техника может повредить данным, создать нагрузку, извлечь секреты, перебрать учетные записи или выйти за scope, она не переносится на Slider AI и остается только в lab-only/cloud-lab формате.

Такой подход важен для повышения квалификации QA: цель не “запустить хакерский инструмент”, а научиться отвечать за безопасность продукта так же дисциплинированно, как за функциональное качество. В каждом упражнении студент должен видеть разрешенную цель, среду выполнения, ожидаемый результат, критерий остановки и sanitized evidence.

## Теория

### 1. Предмет урока: Занятие 2: Терминал Linux — базовые команды

Этот блок строит рабочую дисциплину: студент учится управлять локальной средой, файлами, процессами и evidence так, чтобы дальнейшие security-проверки были воспроизводимыми. В этом уроке центральная модель: shell, аргументы команды, текущая директория, stdout/stderr и безопасное перенаправление. Студент должен понять ее внутри самого Markdown-файла, без необходимости искать базовую теорию в книгах или внешних статьях.

Книги курса используются как источники автора: они дают системность, терминологию и инженерный взгляд. Но учебное действие здесь выполняется в лекции: понятие объясняется, затем показывается безопасный пример, затем студент делает минимальную практику и оформляет результат как evidence.

### 2. Модель и границы: Терминал Linux — базовые команды

Модель `shell, аргументы команды, текущая директория, stdout/stderr и безопасное перенаправление` нужно читать как набор связанных элементов, а не как список слов. В каждом упражнении есть субъект действия, разрешенная цель, технический механизм, наблюдаемый результат и решение: это `observation`, `finding`, `not applicable`, `not reproducible` или `requires approval`.

Граница безопасности для этого урока: запрещено удаление, перезапись или перемещение файлов вне учебной папки. Если такое действие технически нужно для обучения, оно переносится в lab-only/cloud lab или формулируется как запрос approval. Для Slider AI используется только `https://olddev.slider-ai.ru` и только действия, совместимые с правилами Slider AI olddev из пользовательской инструкции курса.

### 3. Ключевые понятия: Терминал Linux — базовые команды

`Target` — конкретная разрешенная цель: локальная папка, localhost, учебная лаборатория, cloud lab или olddev-стенд.

`Mechanism` — технический механизм урока: shell, аргументы команды, текущая директория, stdout/stderr и безопасное перенаправление. Его нужно объяснить до команды или инструмента.

`Expected result` — заранее понятный безопасный результат. Если студент не знает, что должно измениться или появиться в выводе, практика еще не готова.

`Evidence` — минимальный sanitized артефакт: среда, цель, действие, фактический результат, интерпретация и следующий шаг.

`Stop condition` — условие остановки. Оно срабатывает при ошибке scope, появлении секретов/PII, признаках нагрузки, необходимости перебора или неоднозначности разрешения.

### 4. SDET-практика: Терминал Linux — базовые команды

SDET подходит к теме как к тестируемому процессу. Сначала формулируется гипотеза: что именно проверяется и почему это влияет на безопасность продукта. Затем выбирается минимальное действие: локальная команда, ручной шаг, DevTools-наблюдение, lab-only payload или safe helper. После этого результат оформляется так, чтобы разработчик, тимлид или security owner мог его повторить.

Минимальный результат урока: создать локальный evidence-файл и объяснить каждую строку вывода. Если результат не достигается, студент должен уметь объяснить, чего не хватает: разрешения, среды, тестовых данных, изученного инструмента или безопасной лаборатории.

### 5. Безопасная среда для урока

Базовая среда курса — macOS native на MacBook Air M2. Для macOS используются Homebrew, официальные installers, браузер, DevTools, Burp/ZAP в безопасном режиме, Python и локальные файлы. Команды Linux/Kali должны быть явно помечены как Kali/Linux или cloud lab.

Kali ARM64 VM используется как углубление, когда нужна изоляция, Kali-специфичный инструмент или экзаменационная практика. На 8GB RAM VM ограничивается 3-4GB RAM и 2 CPU. Тяжелые сценарии и CTF выполняются в TryHackMe AttackBox, HackTheBox/Pwnbox, PortSwigger Academy или аналогичной легальной среде.

### 6. Как читать результат в этой теме

Результат читается через контрольные признаки, относящиеся именно к теме урока. Для команд это target, статус выполнения, ключевая строка вывода и ошибка. Для HTTP это method, path, status code, selected headers и безопасно очищенный body fragment. Для Python helper это входные параметры, отказ policy, timeout, status и JSON/Markdown output. Для процессных документов это scope, owner, evidence, severity, remediation и retest.

Вывод инструмента или команды не является автоматическим finding. Сначала это observation. Finding появляется только после проверки контекста, влияния, воспроизводимости и границ разрешения.

### 7. Типичные ошибки: Терминал Linux — базовые команды

Первая ошибка — выполнять практику до понимания модели `shell, аргументы команды, текущая директория, stdout/stderr и безопасное перенаправление`. Это превращает обучение в копирование команд.

Вторая ошибка — не отделять lab-only от product-safe. То, что разрешено в CTF или deliberately vulnerable VM, не становится разрешенным на Slider AI.

Третья ошибка — сохранять лишние данные. Evidence не должен содержать cookies, токены, пароли, приватные ключи, персональные данные, полные ответы с секретами или чужие данные.

Четвертая ошибка — путать observation и finding. Наблюдение полезно, но оно требует интерпретации и triage.

### 8. Связь с книгами и SDET-базой

Из SDET Python QA Automation Apprenticeship сюда переносится дисциплина: входные требования, повторяемые шаги, фактический результат, тестовые артефакты, отчетность и регрессия. Из книг курса автор берет системность и инженерный язык, но не перекладывает обучение на студента.

Студент должен выйти из урока с рабочим пониманием: что такое shell, аргументы команды, текущая директория, stdout/stderr и безопасное перенаправление, как это безопасно проверить, что приложить как evidence и какие действия требуют approval.

### 9. Проверка понимания перед практикой

Перед практикой студент отвечает на пять вопросов:

1. Какая цель разрешена?
2. Какой механизм урока используется?
3. Какой результат ожидается?
4. Где stop condition?
5. Что будет приложено как sanitized evidence?

Если на один вопрос нет ответа, практика не выполняется. Сначала уточняется scope, выбирается lab или уменьшается действие до безопасного наблюдения.

### 10. Минимальная планка по уроку

Лекция считается освоенной, если студент может объяснить `shell, аргументы команды, текущая директория, stdout/stderr и безопасное перенаправление` своими словами, выполнить безопасный путь новичка, получить ожидаемый вывод, интерпретировать его и оформить evidence. Для темы `Занятие 2: Терминал Linux — базовые команды` минимальная сдача — создать локальный evidence-файл и объяснить каждую строку вывода, без нарушения ограничения: удаление, перезапись или перемещение файлов вне учебной папки.


### 11. Разбор учебного артефакта

В каждом уроке студент должен уметь показать не только выполненное действие, но и его смысл. Артефакт читается так: сначала указывается среда, затем разрешенная цель, затем действие, затем фактический результат, затем интерпретация. Если артефактом является команда, важно сохранить саму команду и 3-10 строк вывода. Если артефактом является браузерное наблюдение, сохраняется путь в UI, статус запроса, выбранные headers или sanitized screenshot. Если артефактом является документ процесса, в нем должны быть scope, owner, ограничения, evidence и следующий шаг.

Ошибкой считается артефакт без интерпретации. Например, строка вывода сама по себе не доказывает навык. Студент должен объяснить, почему строка важна, какой риск она подтверждает или не подтверждает, и что команда не нарушила scope. Такой формат делает курс самодостаточным: студент учится прямо в лекции читать результат, а не искать объяснение в другом источнике.

### 12. Минимум, Slider AI и углубление

Обязательный минимум выполняется в безопасной среде: macOS native, локальный файл, localhost, браузер, DevTools или одиночное разрешенное наблюдение. Этот минимум нужен для формирования базового навыка без лишней когнитивной нагрузки. В нем не должно быть действий, которые могут повлиять на чужие данные, вызвать нагрузку или потребовать специальных разрешений.

Практика Slider AI всегда ограничена `https://olddev.slider-ai.ru` и правилами Slider AI olddev из пользовательской инструкции курса. Если тема урока потенциально опасна, Slider AI-задание формулируется как наблюдение, checklist, evidence draft, request for approval или transfer note. Углубление выполняется только после изучения следующих уроков и только в lab/cloud lab, где правила явно разрешают такие действия.

### 13. Критерии академической сдачи

Зачет по уроку требует четырех вещей: студент объясняет модель темы своими словами, выполняет безопасный путь, получает ожидаемый результат и оформляет sanitized evidence. Хороший уровень добавляет анализ ограничений: что не проверялось и почему. Отличный уровень добавляет перенос в SDET ownership: как превратить результат в regression check, finding draft, retest step или improvement для security process.

Если студент может только повторить команду, урок не засчитан как самостоятельное освоение. Если студент понимает, когда остановиться и как запросить approval, это считается частью профессиональной квалификации. Именно это отличает курс повышения квалификации QA/SDET от набора разрозненных pentest-рецептов.


### 14. Самостоятельное объяснение без внешних источников

После чтения лекции студент должен суметь пересказать тему человеку из команды, который не присутствовал на занятии. Такое объяснение должно включать четыре части. Первая часть — техническая модель: какие компоненты участвуют и почему они важны. Вторая часть — безопасная демонстрация: какое минимальное действие показывает работу модели без риска для продукта. Третья часть — evidence: какие строки, скриншоты или поля документа подтверждают результат. Четвертая часть — границы: что нельзя делать на olddev без approval и куда переносится углубление.

Этот пересказ является обязательной частью самодостаточности курса. Книги остаются источниками автора, но рабочее знание, необходимое для выполнения заданий, находится внутри урока: термины, причина, пример, безопасная команда или ручной шаг, эталон вывода и критерии сдачи.

### 15. Перенос в профессиональную работу SDET/Security QA

В реальной команде результат урока должен превращаться в рабочее действие: test case, checklist item, automation helper, finding draft, retest step или regression guard. Поэтому студент фиксирует не только “что получилось”, но и “как это поможет команде снизить риск”. Если проверка ничего не выявила, это тоже полезный результат, но он оформляется как `observation` или `not applicable`, а не как найденная уязвимость.

Профессиональная зрелость проявляется в умении остановиться. Если дальнейший шаг требует перебора, активного сканирования, payload, доступа к чужим данным, изменения состояния или анализа секретов, студент не выполняет его на Slider AI. Он записывает limitation, выбирает lab-only среду или оформляет approval request. Такая дисциплина делает курс пригодным для самообразования и для повышения квалификации QA, потому что учит не только технике, но и ответственности за безопасность продукта.

## Guided practice

1. Работайте только в `~/slider-ai-pentest`.
2. Создайте отдельную папку `evidence/lesson_02`.
3. Выполните команды на локальных файлах.
4. Сохраните вывод в `notes/lesson_02_terminal.md`.
5. Для каждой команды напишите, что она подтверждает.

### Эталон самостоятельной работы

К концу guided practice студент может открыть `notes/lesson_02_terminal.md` и увидеть не список тем, а понятный отчет:

- где выполнялась работа;
- какие команды были выполнены;
- какой вывод получен;
- какой вывод подтверждает успешность;
- какие действия были локальными и безопасными.

## Практическое занятие

### Шаг 1: Подготовить папку урока

```bash
cd ~/slider-ai-pentest
mkdir -p evidence/lesson_02
touch notes/lesson_02_terminal.md
pwd
```

Пример вывода:

```text
/Users/student/slider-ai-pentest
```

### Шаг 2: Сохранить базовую информацию о среде

```bash
{
  echo "# Lesson 02 — Terminal evidence"
  echo
  echo "Environment: macOS native"
  echo "Target: local workspace only"
  echo
  echo "## Commands"
  echo
  echo '```text'
  pwd
  uname -m
  whoami
  echo '```'
} > notes/lesson_02_terminal.md
```

Пояснение: фигурные скобки группируют несколько команд, а `>` записывает общий вывод в файл. Это локальная операция, она не обращается к Slider AI.

### Шаг 3: Создать учебные файлы

```bash
cd ~/slider-ai-pentest/evidence/lesson_02
touch report1.txt report2.txt report3.txt
echo "Status: observation" > report1.txt
echo "Status: not applicable" > report2.txt
echo "Status: requires approval" > report3.txt
ls -lah
```

Пример вывода:

```text
total 24
drwxr-xr-x  5 student  staff  160 May 22 10:20 .
drwxr-xr-x  3 student  staff   96 May 22 10:20 ..
-rw-r--r--  1 student  staff   20 May 22 10:20 report1.txt
-rw-r--r--  1 student  staff   24 May 22 10:20 report2.txt
-rw-r--r--  1 student  staff   26 May 22 10:20 report3.txt
```

Что это подтверждает: файлы созданы в правильной папке, у них есть обычные пользовательские права, работа идет локально.

### Шаг 4: Читать файлы

```bash
cat report1.txt
cat -n report2.txt
head -1 report3.txt
```

Пример вывода:

```text
Status: observation
     1	Status: not applicable
Status: requires approval
```

Что это подтверждает: студент умеет читать маленькие evidence-файлы и видеть содержимое без редактора.

### Шаг 5: Копировать и перемещать

```bash
mkdir archive
cp report1.txt archive/report1.copy.txt
mv report2.txt archive/report2.moved.txt
ls -lah
ls -lah archive
```

Как читать результат:

- `report1.txt` должен остаться на месте, потому что `cp` копирует;
- `report2.txt` должен исчезнуть из текущей папки и появиться в `archive`, потому что `mv` перемещает;
- новое имя `report2.moved.txt` показывает, что `mv` также умеет переименовывать.

### Шаг 6: Фильтровать вывод

```bash
grep -R "Status" .
grep -R "requires approval" .
```

Пример вывода:

```text
./report1.txt:Status: observation
./report3.txt:Status: requires approval
./archive/report1.copy.txt:Status: observation
./archive/report2.moved.txt:Status: not applicable
./report3.txt:Status: requires approval
```

Что это подтверждает: `grep -R` ищет по текущей папке и подпапкам. Это удобно для заметок, но на реальном продукте нельзя бездумно искать и сохранять секреты.

### Шаг 7: Добавить результат в заметку

```bash
{
  echo
  echo "## Evidence summary"
  echo
  echo '```text'
  ls -lah ~/slider-ai-pentest/evidence/lesson_02
  echo '```'
  echo
  echo "Interpretation: local evidence files were created, read, copied and moved safely."
  echo "Result status: observation"
} >> ~/slider-ai-pentest/notes/lesson_02_terminal.md
```

Обратите внимание на `>>`: мы добавляем новый блок в конец заметки, а не перезаписываем уже записанную информацию.

### Шаг 8: Безопасное удаление только временного файла

```bash
cd ~/slider-ai-pentest/evidence/lesson_02
touch tmp-delete-me.txt
pwd
ls -lah tmp-delete-me.txt
rm -i tmp-delete-me.txt
```

Если shell спросит подтверждение, введите `y` только после проверки пути и имени файла.

## Примеры вывода

Минимальный эталонный артефакт для сдачи по теме `Занятие 2: Терминал Linux — базовые команды`:

```markdown
Environment: macOS native, Apple Silicon
Target: https://olddev.slider-ai.ru
Scope status: allowed observation within пользовательской инструкцией курса
Action: safe manual or low-impact check from this lesson
Evidence:
  - Command or browser path is recorded.
  - Output contains only sanitized technical lines.
  - Cookies, tokens, passwords and personal data are not stored.
Observed result: baseline behavior captured without destructive action
Interpretation: observation; no vulnerability is confirmed without additional proof
Next step: document result, request approval for intrusive follow-up, or repeat in lab-only environment
```

Пример локального вывода для обязательного безопасного пути:

```text
Environment: macOS native
Target: local workspace or explicitly allowed olddev observation
Result status: observation
Evidence saved: evidence/current_lesson/notes.md
Sanitization: secrets and personal data excluded
```

Такой вывод считается эталонным не потому, что строки всегда будут идентичными, а потому что в нем есть все обязательные элементы профессионального evidence: среда, разрешенная цель, действие, наблюдение, интерпретация, sanitization и следующий безопасный шаг.

## Адаптация под macOS (M2, 8GB)

- На macOS используйте Terminal и `zsh`; для этого урока не нужна Kali VM.
- Часть флагов Linux-команд может отличаться на macOS, потому что многие системные утилиты имеют BSD-варианты.
- Если команда из Linux-примера не совпадает, фиксируйте это как troubleshooting-наблюдение, а не как провал.
- Не устанавливайте GNU-версии команд только ради первых уроков.
- Kali ARM64 VM используйте позже для Linux-специфичных упражнений, ограничив RAM до 3-4GB.

## Задачи для самостоятельного выполнения

1. **Минимум:** Создайте `evidence/lesson_02`, три файла `report1.txt`, `report2.txt`, `report3.txt` и папку `archive`.
2. **Минимум:** Сохраните в `notes/lesson_02_terminal.md` вывод `pwd`, `ls -lah`, `cat`, `grep`.
3. **Минимум:** Объясните разницу между `>`, `>>` и `|` своими словами.
4. **Практика Slider AI:** Подготовьте локальный файл `requests/slider_ai_scope_note.txt` с одной строкой: `Allowed target: https://olddev.slider-ai.ru; no product testing in lesson 02`.
5. **Углубление:** В Kali/Ubuntu VM повторите только локальную часть упражнения и запишите 3 отличия от macOS.
6. **Безопасность:** Напишите чек-лист из 4 пунктов, который нужно выполнить перед `rm`, `mv` и `>`.

## Частые ошибки

1. **Выполнить команду не в той папке.** Лечится привычкой `pwd` перед важным действием.
2. **Перезаписать evidence через `>`.** Для добавления используйте `>>`.
3. **Думать, что `mv` только перемещает.** `mv` также переименовывает и может затереть целевой файл.
4. **Открывать большой файл через `cat`.** Для больших файлов используйте `less`, `head`, `tail`.
5. **Использовать `rm -rf` как привычку.** В учебном курсе сначала используется `rm -i` и только локальные временные файлы.

## Вопросы на понимание

1. Чем терминал отличается от shell?
2. Почему перед удалением или перемещением нужно выполнить `pwd`?
3. Чем отличается абсолютный путь от относительного?
4. Что делает `ls -lah`?
5. Почему `>` опаснее, чем `>>`, если файл уже содержит evidence?
6. Как пайп `|` помогает читать вывод команд?
7. Почему в этом уроке мы не тестируем Slider AI, хотя записываем его URL?

## Практика на Slider AI

**Цель стенда:** `https://olddev.slider-ai.ru`

**Контекст разрешения:** тестовый стенд проекта Slider AI, доступен QA для обучения и проверки безопасности. Production и любые другие домены не входят в это задание.

**Ограничения безопасности:** соблюдать правилами Slider AI olddev из пользовательской инструкции курса; не выполнять DoS/load-тесты, brute force, destructive payloads, изменение чужих данных, извлечение секретов и действия вне согласованного scope.

**Уровень прогрессии:** локальная подготовка evidence без обращения к приложению.

### Минимум

Создайте файл `notes/lesson_02_terminal.md`, добавьте дату, локальную среду, цель стенда и список будущих типов артефактов.

### Практика Slider AI

Создайте локальный файл:

```bash
cd ~/slider-ai-pentest
echo "Allowed target: https://olddev.slider-ai.ru; no product testing in lesson 02" > requests/slider_ai_scope_note.txt
cat requests/slider_ai_scope_note.txt
```

Это не проверка сайта. Это подготовка локальной дисциплины: URL стенда записан, границы понятны, никаких запросов к приложению не выполняется.

### Углубление после изучения следующих уроков

После урока 6 добавьте поиск по заметкам и быстрый список всех файлов доказательств:

```bash
find ~/slider-ai-pentest -type f
grep -R "requires approval" ~/slider-ai-pentest/notes
```

Эти команды выполняются только по локальному workspace.

### Артефакт сдачи

Markdown-запись по шаблону:

```markdown
Lesson: 02
Component: terminal evidence workflow
Target: local workspace; Slider AI URL recorded for future scoped work
Steps: created files, copied/moved files, read content, filtered local evidence
Actual result: evidence files exist and are readable
Evidence: sanitized terminal output
Risk: accidental overwrite or deletion if commands are used carelessly
Recommendation: use pwd + ls before destructive file operations
Status: observation
```

### Критерий готовности

Задание выполнено локально, не отправляет запросы к Slider AI, содержит проверяемый артефакт и явно отмечает `observation`.

## Rubric

| Уровень | Что должно быть сдано |
|---|---|
| Зачет | Выполнен обязательный путь новичка, есть sanitized evidence, команды выполнены локально |
| Хорошо | Есть объяснение `>`, `>>`, `|`, `cp`, `mv`, `grep` и безопасного удаления |
| Отлично | Подготовлен чек-лист терминальной безопасности для будущих security-проверок |

## Self-check

1. Какая SDET-компетенция используется в уроке?
2. Какая часть объяснения опирается на книги курса?
3. Где проходит безопасная граница для Slider AI?
4. Какой артефакт можно показать команде без раскрытия секретов?
5. Что нужно вынести в углубление, lab-only или отдельный approval?
