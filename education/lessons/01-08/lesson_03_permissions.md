# Занятие 3: Права доступа в Linux

## Учебная рамка

**Входные требования:** Уверенное выполнение команд из предыдущих занятий; доступ к терминалу macOS/Linux или учебной VM.

**Результат занятия:** Студент выполняет базовую операцию Linux, объясняет назначение команд и фиксирует результат в заметках.

**Наследуемая SDET-компетенция:** профессиональная рабочая среда, evidence workspace, терминальная дисциплина и воспроизводимые команды.

**Security QA-компетенция:** подготовка безопасной рабочей станции и локального evidence-контура.

**Связь с книгами:** «PyCharm. Профессиональная работа на Python 2024» — terminal, Git, Markdown evidence и рабочая станция; SDET-курс — IDE/venv/terminal discipline.

**Основной источник:** «PyCharm. Профессиональная работа на Python 2024» и «Легкий способ выучить Python 3 еще глубже».

**Дополнительные источники:** «Объектно-ориентированный Python» для аккуратной структуры локальных helpers.

**Что берем из источника:** terminal discipline, работа с файлами, Markdown evidence, локальная организация проекта, базовые CLI-навыки.

**Как это превращается в SDET/Security QA навык:** превратить рабочую станцию QA в управляемую среду для безопасного сбора артефактов.

**Что нельзя переносить на Slider AI без отдельного разрешения:** не выполнять активные проверки продукта; только подготовка среды и локальные артефакты.


**Процессный артефакт:** `SECURITY_TEST_STRATEGY.md` или `EVIDENCE_POLICY.md`: заметка о рабочей среде и правилах evidence.

**Безопасная цель:** Локальная домашняя директория студента, учебная VM Kali/Ubuntu или специально созданная папка `~/pentest-lab`. Не использовать чужие системы.

**Среда выполнения:** Основной путь — macOS native, браузер, DevTools, Homebrew и Python. Kali Linux ARM64 VM, UTM или cloud lab используются только если это явно требуется задачей или вынесено в углубление.

**Обязательный путь новичка:** Повторить команды из урока на локальных файлах, записать 3-5 наблюдений и сохранить вывод ключевых команд.

**Углубление:** Выполнить дополнительное задание в отдельной VM, сравнить поведение Linux и macOS, оформить короткий troubleshooting-разбор.

**Минимальная проверка успеха:** Команды из практики выполнены без ошибок, создан ожидаемый файл/каталог/вывод, студент может повторить действие по памяти.

**Эталонный вывод:** В отчете есть команда, ожидаемый вывод терминала и короткое объяснение, что этот вывод подтверждает.

**Критерии сдачи:** Зачет: выполнен обязательный путь, приложен вывод, нет действий вне учебной среды. Отлично: выполнено углубление и описана одна типичная ошибка.

## Reading pack из книг курса

Этот раздел не является заданием “пойди и найди теорию в книгах”. Книги использованы автором курса для подготовки лекции `Занятие 3: Права доступа в Linux`, а студент получает самодостаточное объяснение в разделах `Source-driven theory` и `Теория`.

- `docs/socraticode/pycharm-professional-python-2024-pages/`
- `docs/socraticode/learn-more-python-3-pages/`

Конкретные страницы для этого блока: `pycharm-professional-python-2024-pages/page-178.md`-`page-209.md`; `page-577.md`-`page-590.md`; `learn-more-python-3-pages/page-001.md`-`page-060.md`.

Что обязана объяснить лекция на основе этих книг:

1. Термины и команды, которые прямо поддерживают тему урока.
2. Инженерный принцип, который переносится из SDET в Security QA.
3. Ограничение безопасности: что нельзя делать на Slider AI без approval.
4. Пример, который превращается в evidence, helper, checklist или process artifact.

Если книга описывает опасную технику, она переносится только в lab-only или defensive interpretation. Студент не должен обращаться к книгам, чтобы понять базовую теорию текущего урока.

## Source-driven theory

Этот урок опирается на книжные источники курса как на базу, а не как на факультативное чтение. Из источников берется практическая дисциплина: terminal discipline, работа с файлами, Markdown evidence, локальная организация проекта, базовые CLI-навыки. Для SDET это важно потому, что security-проверка должна быть воспроизводимой, объяснимой и пригодной для отчета, а не превращаться в набор разрозненных команд.

Книжный материал в уроке используется в трех шагах:

1. Понять термин или технику на безопасном примере.
2. Перевести идею в QA-действие: test case, observation, evidence, helper или process artifact.
3. Отделить разрешенную практику от действий, которые требуют отдельного approval.

Граница для Slider AI: не выполнять активные проверки продукта; только подготовка среды и локальные артефакты. Если нужная техника выходит за эту границу, результат урока оформляется как `requires approval`, lab-only practice или defensive recommendation.

## Теория

В Linux каждый файл и директория имеют права доступа, определяющие кто может читать, записывать и выполнять их.

> **Примечание для macOS (M2, 8GB):** Для запуска Kali Linux на Mac с процессором M2 используйте UTM или Parallels Desktop вместо VirtualBox. Выделяйте виртуалке не более 3-4GB RAM при 8GB на хосте. Установка пакетов в Kali выполняется через `sudo apt install`, для нативных инструментов macOS используйте `brew install`.

### Типы прав доступа

Права обозначаются тремя группами по три бита: `rwx` (read, write, execute)

```
-rwxr-xr--  1 user group  4096 file.txt
↑  ↑↑↑ ↑↑↑ ↑↑↑
│   │││ │││ │││
│   │││ │││ └└└─ права для "others" (остальные)
│   │││ ││└──── права для "group" (группа)
│   │││ └└───── права для "owner" (владелец)
│   └└└─────── тип файла и права
└───────────── тип файла (- обычный, d директория, l ссылка)
```

### Числовое представление (octal)

| Бит | Значение | Число |
|-----|----------|-------|
| r   | чтение   | 4     |
| w   | запись   | 2     |
| x   | выполнение | 1   |

Примеры:
- `755` = rwxr-xr-x (владелец всё, остальные только чтение и выполнение)
- `644` = rw-r--r-- (владелец читает/пишет, остальные только читают)
- `700` = rwx------ (только владелец имеет доступ)

### Ключевые команды

| Команда | Назначение |
|---------|-----------|
| `chmod` | Изменить права доступа |
| `chown` | Изменить владельца и группу |
| `sudo` | Выполнить команду от имени root |
| `su` | Переключиться на другого пользователя |
| `id` | Показать UID, GID, группы |
| `whoami` | Показать текущего пользователя |

### SUID, SGID, Sticky bit

- **SUID** (4000) — программа выполняется с правами владельца (например, `/usr/bin/passwd`)
- **SGID** (2000) — программа выполняется с правами группы
- **Sticky bit** (1000) — в директории (например `/tmp`) файлы может удалить только владелец

## Guided practice

1. Создайте или проверьте рабочую папку `~/security-qa-workspace` и отдельную заметку урока.
2. Выполните команды урока на локальной машине или учебном lab-target, не затрагивая production.
3. Сохраните минимальный вывод команды в evidence-файл без секретов и персональных данных.
4. Добавьте 3-5 предложений: что проверено, что означает результат и как это пригодится Security QA.

### Эталон самостоятельной работы

К концу guided practice у студента есть короткий Markdown-артефакт: цель проверки, выполненные шаги, sanitized evidence, интерпретация результата, границы применимости и следующий безопасный шаг.

## Практическое занятие

### Шаг 1: Проверка текущего пользователя и прав

```bash
# Кто я?
whoami
```
**Пример вывода `whoami`:**
```text
user
```

```bash
# Мои права и группы
id
```
**Пример вывода `id`:**
```text
uid=1000(user) gid=1000(user) groups=1000(user),27(sudo)
```

```bash
# Список пользователей в системе
cat /etc/passwd
```
**Пример вывода `cat /etc/passwd` (фрагмент):**
```text
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
user:x:1000:1000:user:/home/user:/bin/bash
```

```bash
# Список групп
cat /etc/group
```

### Шаг 2: Просмотр и изменение прав (chmod)

```bash
cd ~/terminal_practice

# Создать тестовый файл
echo "secret data" > secret.txt

# Посмотреть права
ls -l secret.txt
```
**Пример вывода `ls -l secret.txt`:**
```text
-rw-r--r-- 1 user user 12 Jan 10 10:00 secret.txt
```

```bash
# Убрать все права для others
chmod o-rwx secret.txt
ls -l secret.txt
```
**Пример вывода:**
```text
-rw-r----- 1 user user 12 Jan 10 10:00 secret.txt
```

```bash
# Дать все права владельцу, никаких для остальных
chmod 700 secret.txt
ls -l secret.txt
```
**Пример вывода:**
```text
-rwx------ 1 user user 12 Jan 10 10:00 secret.txt
```

```bash
# Сделать файл исполняемым
echo '#!/bin/bash' > script.sh
echo 'echo "Hello"' >> script.sh
chmod +x script.sh
ls -l script.sh
```
**Пример вывода:**
```text
-rwxr-xr-x 1 user user 24 Jan 10 10:01 script.sh
```

```bash
./script.sh
```
**Пример вывода:**
```text
Hello
```

### Шаг 3: Изменение владельца (chown)

```bash
# Посмотреть текущего владельца
ls -l secret.txt

# Создать тестового пользователя (если нет)
sudo useradd testuser

# Изменить владельца файла
sudo chown testuser secret.txt
ls -l secret.txt
```
**Пример вывода:**
```text
-rwx------ 1 testuser user 12 Jan 10 10:00 secret.txt
```

```bash
# Изменить владельца и группу
sudo chown testuser:testuser secret.txt
ls -l secret.txt

# Вернуть себе
sudo chown $USER:$USER secret.txt
```

### Шаг 4: Работа с sudo и su

```bash
# Выполнить команду от root
sudo cat /etc/shadow
```
**Пример вывода `sudo cat /etc/shadow` (фрагмент):**
```text
root:$6$abc123$...:19000:0:99999:7:::
user:$6$def456$...:19000:0:99999:7:::
```

```bash
# Переключиться на root (нужен пароль root)
sudo -i
# или
su -
# Выйти из root
exit

# Посмотреть, кто может использовать sudo
cat /etc/sudoers
sudo cat /etc/sudoers
```

### Шаг 5: SUID и поиск уязвимостей

> **Примечание:** Команда `find` подробно изучается в Уроке 6. Кратко: `find` ищет файлы по файловой системе по заданным критериям.

```bash
# Найти все файлы с SUID-битом (потенциальные векторы повышения привилегий)
find / -perm -4000 2>/dev/null
```
**Пример вывода:**
```text
/usr/bin/passwd
/usr/bin/sudo
/usr/bin/su
```

```bash
# Посмотреть SUID у конкретного файла
ls -l /usr/bin/passwd
```
**Пример вывода:**
```text
-rwsr-xr-x 1 root root 68208 Jan 10 09:00 /usr/bin/passwd
```

```bash
# Найти файлы с правами записи для всех
find / -perm -002 2>/dev/null | head -20
```


## Примеры вывода

Минимальный эталонный артефакт для сдачи:

```markdown
Environment: macOS native / Kali ARM64 VM / cloud lab / Slider AI olddev
Target: <разрешенная учебная цель или https://olddev.slider-ai.ru>
Action: <выполненная безопасная команда или ручной шаг>
Evidence: <санитизированный фрагмент вывода, скриншота или HTTP history>
Result status: finding / observation / not reproducible / not applicable / requires approval
Next step: <retest, remediation, approval request или lab-only follow-up>
```

В отчете студент указывает среду выполнения, безопасную цель, команду или ручные шаги и коротко объясняет, какая строка подтверждает результат.



## Адаптация под macOS (M2, 8GB)

- Для macOS native используйте Homebrew или официальный installer: `brew install <tool>`; для явно помеченной Kali/Linux-среды допустим `apt`.
- Kali/Linux VM запускайте только как углубление и выделяйте не более 3-4GB RAM на MacBook Air M2 (8GB)
- Если нужна Kali/Linux VM на Apple Silicon, используйте ARM64-образ в UTM/VMware Fusion/Parallels; не используйте x86/x64 VM как базовый путь.
- Docker работает нативно на M2: `docker pull <image>`
- Для VPN используйте Tunnelblick (OpenVPN) или официальные клиенты
- Для Python используйте `pip3 install` вместо `pip install`


## Задачи для самостоятельного выполнения

1. Создайте файл `private.txt` и установите права `600` (только владелец может читать и писать). Проверьте, что другой пользователь (или от имени другого пользователя через `su`) не может прочитать файл.

2. Создайте директорию `shared/` и установите права `775`. Создайте внутри файл `team.txt` с правами `664`. Объясните, почему выбраны такие права.

3. Найдите в системе все файлы с установленным SUID-битом. Сохраните список в `~/suid_files.txt`. *Примечание: используется команда `find`, которая подробно изучается в Уроке 6. Краткий синтаксис: `find / -perm -4000 2>/dev/null`.* Исследуйте, какие из них могут быть использованы для повышения привилегий (nmap, find, vim с SUID — критично).

4. Создайте скрипт `backup.sh`, который архивирует директорию `~/practice` в `~/backup.tar.gz`. Сделайте скрипт исполняемым только для владельца. Запустите скрипт через `sudo` и убедитесь, что архив создан. *Примечание: создание bash-скриптов подробно изучается в Уроке 7. Кратко: скрипт начинается с `#!/bin/bash` и делается исполняемым через `chmod +x`.*

5. Исследуйте файл `/etc/sudoers` и файлы в `/etc/sudoers.d/`. Узнайте, как дать пользователю право запускать конкретную команду без ввода пароля. Напишите пример строки для sudoers.

## Частые ошибки

1. **Неправильный подсчет прав chmod.** Часто забывают, что r=4, w=2, x=1. Например, 777 дает все права всем, что опасно для системных файлов.
2. **Использование chown без sudo.** Обычный пользователь не может менять владельца файлов, принадлежащих root.
3. **Случайная установка SUID-бита на пользовательские скрипты.** Это позволит любому запускать скрипт с правами владельца, создавая уязвимость.
4. **Путаница между `su` и `sudo`.** `su` переключает пользователя (требует пароль целевого пользователя), `sudo` выполняет команду от root (требует пароль текущего пользователя).

## Вопросы на понимание

1. Что означают права доступа `rwxr-xr--` для файла?
2. Какое числовое значение соответствует правам `rw-r-----`?
3. Зачем нужен SUID-бит у программы `/usr/bin/passwd`?
4. Чем отличается команда `sudo` от `su -`?
5. Почему директория `/tmp` имеет Sticky bit?

## Практика на Slider AI

**Цель стенда:** `https://olddev.slider-ai.ru`

**Контекст разрешения:** тестовый стенд проекта Slider AI, доступен QA для обучения и проверки безопасности. Production и любые другие домены не входят в это задание.

**Ограничения безопасности:** соблюдать `education/slider_ai_scope.md`; не выполнять DoS/load-тесты, brute force, destructive payloads, изменение чужих данных, извлечение секретов и действия вне согласованного scope.

**Уровень прогрессии:** Безопасное хранение артефактов

### Минимум

Проверьте права на папку `~/slider-ai-pentest` и убедитесь, что артефакты не доступны другим пользователям локальной машины.

### Практика Slider AI

Создайте файл `evidence/permissions_check.txt` с выводом `ls -la` и коротким объяснением прав доступа.

### Углубление после изучения следующих уроков

После урока 7 напишите скрипт, который проверяет права на все новые файлы отчета.

### Артефакт сдачи

Markdown-запись по шаблону из `education/slider_ai_scope.md`: урок, компонент Slider AI, шаги, фактический результат, доказательства без секретов, риск, рекомендация и статус.

### Критерий готовности

Задание выполнено только на `olddev.slider-ai.ru`, не выходит за scope, содержит проверяемый артефакт и явно отмечает `finding`, `informational`, `not reproducible`, `not applicable` или `requires approval`.

## Rubric

| Уровень | Что должно быть сдано |
|---|---|
| Зачет | Выполнен обязательный путь новичка, есть sanitized evidence, действия не выходят за scope |
| Хорошо | Есть объяснение риска или процесса, аккуратные шаги воспроизведения и корректный статус результата |
| Отлично | Результат связан с `Security QA workstation readiness`, remediation/retest или automation appendix |

## Self-check

1. Какая SDET-компетенция используется в уроке?
2. Какая часть объяснения опирается на книги курса?
3. Где проходит безопасная граница для Slider AI?
4. Какой артефакт можно показать команде без раскрытия секретов?
5. Что нужно вынести в углубление, lab-only или отдельный approval?
